vars:
  VM_ENV: prod

policies:
  - name: stop-ec2
    resource: ec2
    filters:
      - type: value
        key: "tag:Environment"
        value: "prod"
      - type: value
        key: State.Name
        value: running
    actions:
      - stop
