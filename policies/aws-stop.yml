policies:
  - name: stop-ec2
    resource: ec2
    filters:
      - type: value
        key: "tag:VM"
        value: "${VM}"
      - type: value
        key: State.Name
        value: running
    actions:
      - stop
