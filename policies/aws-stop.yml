policies:
  - name: stop-ec2
    resource: ec2
    filters:
      - type: value
        key: "tag:Environment"
        value: "{{ env.VM_ENV }}"
      - type: value
        key: State.Name
        value: running
    actions:
      - stop
