policies:
  - name: start-ec2
    resource: ec2
    filters:
      - type: value
        key: "tag:VM"
        value: "${VM}"
      - type: value
        key: State.Name
        value: stopped
    actions:
      - start
